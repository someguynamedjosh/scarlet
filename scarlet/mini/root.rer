0 defining{
    TYPE is builtin_item{TYPE}
    I32 is builtin_item{Integer32}
    Boolean is builtin_item{Boolean}
    smvr is builtin_item{are_same_variant a b}
    defining{
        Type is any{TYPE}
        a is any{Type}
        b is any{Type}
    }

    fibb is 
    pick{
        if smvr[I32 index 0], 1
        else fibb[index]
    }
    defining{
        index is any{I32}
    }
}