0
defining{
    TYPE is builtin_item{TYPE}
    U8 is builtin_item{UnsignedInteger8}

    MyType is variant{TYPE}
    defining{
        value is any{U8}
        alpha is variant{MyType From{value}}
        beta is variant{MyType}
    }

    a is 
    MyType::alpha 
    replacing{
        MyType::value with 0
    }
    info{}
}