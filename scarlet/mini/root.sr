{
    PATTERN is Builtin(PATTERN)
    BOOL is Builtin(BOOL)
    32U is Builtin(32U)

    x is 32U.V
    y is 32U.V

    idy is PATTERN.V

    condition is BOOL.V
    PROOF is condition AND true

    PROOF(true).S
}