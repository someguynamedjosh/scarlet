{
    PATTERN is Builtin(PATTERN)
    # BOOL is Builtin(BOOL)
    32U is Builtin(32U)

    length is 32U.V.Eager(All)
    element_type is PATTERN.V.Eager(All)
    ARRAY is Builtin(ARRAY length element_type)

    [{1}.Matched(
        on ARRAY(1 PATTERN).Shy(All) true
        else false
    )].Shown
}
