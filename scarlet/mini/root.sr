{
    PATTERN is Builtin(PATTERN)
    BOOL is Builtin(BOOL)
    32U is Builtin(32U)

    x is 32U.V
    y is 32U.V

    paren is PATTERN.V

    thing is After(x y) { thex is x they is y}
    thing.S
}