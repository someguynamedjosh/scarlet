[
    PATTERN is Builtin(PATTERN)
    BOOL is Builtin(BOOL)
    32U is Builtin(32U)

    container is [
        DATA is PATTERN.V
        data is DATA.V
    ]

    container(32U 123).S

    # PROOF_FIVE is 
    # after{number} 
    # number matches 5 AND true
]