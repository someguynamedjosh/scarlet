{
    PATTERN is Builtin(PATTERN)
    BOOL is Builtin(BOOL)
    32U is Builtin(32U)

    {10 20 30}.Mem(0 true).S

    BOOL.Matched(
        on true 123
        on false 12
        else 456
    ).S
}
