0 defining{
    TYPE is builtin_item{TYPE}
    I32 is builtin_item{Integer32}

    Unit is variant{TYPE From{value}}
    defining{
        Data is any{TYPE}
        value is any{Data}
    }

    ProofEqual is variant{TYPE From{left right}}
    defining{
        Data is any{TYPE}
        left is any{Data}
        right is any{Data}
    }

    ProofEqual info{}

    # ZeroUnit is IntUnit replacing{Unit::b with 1}
    # ZeroUnitAgain is ZeroUnit info{}
}