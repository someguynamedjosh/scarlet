{
    PATTERN is Builtin(PATTERN)
    BOOL is Builtin(BOOL)
    32U is Builtin(32U)

    x is 32U.V
    y is 32U.V

    just is PATTERN.V.Eager(just)

    another is just.V

    # finally is another(another is 123).S

    int is 32U.V

    finally is int(int is 123).S
}