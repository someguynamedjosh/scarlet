TYPE is rover::lang::TYPE
I32 is rover::lang::Integer32

Point is Type{
   new is Self recording{
      x is any{I32} 
   }
}

x is Point::new::x

n is any{I32}

NPoint is Point replacing { x with n }

#  nnew is NPoint::new

# asdf asdf asfd 0123

#=
recount is 
pick{
   if input == 0, 0
   else           recount[input - 1]
}
where{
   input is any{Integer32}
}
=#
