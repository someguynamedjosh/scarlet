TYPE is rover::lang::TYPE

Var is any{TYPE}

another is Var
replacing{
   Var with TYPE
}

# asdf asdf asfd 0123

#=
recount is 
pick{
   if input == 0, 0
   else           recount[input - 1]
}
where{
   input is any{Integer32}
}
=#
