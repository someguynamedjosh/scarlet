
Integer32 is rover::lang::Integer32
#  add is Integer32::add

x is any{Integer32}
y is any{Integer32}

yagain is x replacing{
   x with y
}

example is yagain replacing{ y with i32{123} }

#=
recount is 
pick{
   if input == 0, 0
   else           recount[input - 1]
}
where{
   input is any{Integer32}
}
=#
