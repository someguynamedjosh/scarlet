
Integer32 is rover::lang::Integer32
add is Integer32::add
subtract is Integer32::subtract

example is subtract replacing{
   i32{1}
   i32{2}
}

#=
recount is 
pick{
   if input == 0, 0
   else           recount[input - 1]
}
where{
   input is any{Integer32}
}
=#
