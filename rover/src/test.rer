
Integer32 is rover::lang::Integer32
add is Integer32::add

x is any{Integer32}
y is any{Integer32}
z is any{Integer32}

add3 is add replacing{
   add::left with x
   add::right with add replacing{
      add::left with y
      add::right with z
   }
}

#  example is x replacing { x with y }

#=
recount is 
pick{
   if input == 0, 0
   else           recount[input - 1]
}
where{
   input is any{Integer32}
}
=#
