
#  Integer32 is rover::lang::Integer32
#  sum is Integer32::sum

#  x is any{Integer32}
#  y is any{Integer32}
#  z is any{Integer32}

#  sum3 is sum[x sum[y z]]
#  example is sum3[1 2 3]

Primary is Type{
   variant red :Self
   variant green :Self
   variant blue :Self
}

is_green is any{Primary} is_variant{Primary::green}

thing is is_green[Primary::red]

#=
recount is 
pick{
   if input == 0, 0
   else           recount[input - 1]
}
where{
   input is any{Integer32}
}
=#
