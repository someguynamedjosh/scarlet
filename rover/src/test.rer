
add is rover::lang::Integer32::add

three is add replacing{
   add::left with i32{1}
   add::right with i32{2}
}

#=
recount is 
pick{
   if input == 0, 0
   else           recount[input - 1]
}
where{
   input is any{Integer32}
}
=#
